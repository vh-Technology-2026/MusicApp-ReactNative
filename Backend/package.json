{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "dev:ai": "wrangler dev --remote",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "node scripts/init-db.js",
    "seed:jamendo": "node scripts/seed-jamendo.js",
    "seed:remote": "node scripts/seed-jamendo.js --remote",
    "db:migrate:local": "wrangler d1 migrations apply react-native-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply react-native-db --remote",
    "db:execute:local": "wrangler d1 execute react-native-db --local --command \"SELECT * FROM users;\"",
    "db:execute:remote": "wrangler d1 execute react-native-db --remote --command \"SELECT * FROM users;\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "wrangler": "^3.111.0"
  }
}
